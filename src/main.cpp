#include <glut.h>

int main(int argc, char **argv)
{
    // Set-up graphics
    // Set-up input

    // Initialise Chip 8 system
    // Load game into memory

    // Perform emulation loop
    for (;;)
    {
        // Tick

        // If draw flag set, update screen

        // Store key press state (press and release)
    }
    return 0;
}